<% if signed_in? %>
	<div class="container-fluid">
		<h1>what's to be done?</h1>
		<div class="row-fluid">
			<div class="span6">
				<%= render 'shared/todoitem_form' %>
			</div>
			<div class="span4">
				<%= render 'shared/user_info' %>
			</div>
		</div>
	<div>
<% else %>
	<%= render 'shared/introstub' %>
<% end %>
<div>