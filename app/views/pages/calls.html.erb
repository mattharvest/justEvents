<% if signed_in? %>
	<div class="container-fluid">
		<h1>What's the call?</h1>
		<div class="row-fluid">
			<div class="span6">
				<%= render 'shared/call_form' %>
			</div>
			<div class="span2">
				<%= render 'shared/user_info' %>
			</div>
		</div>
	<div>
<% else %>
	<%= render 'shared/introstub' %>
<% end %>
